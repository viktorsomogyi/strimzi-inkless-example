mode: standalone

persistence:
  enabled: true
  storageClass: "__STORAGE_CLASS__"
  size: 500Gi

resources:
  requests:
    memory: 512Mi

rootUser: "admin"
rootPassword: "password123" # Change this for production!

service:
  type: ClusterIP